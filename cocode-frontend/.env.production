# Production Environment Variables for CoCode
# ============================================
# UPDATE THESE with your actual server URLs before building!

# WebSocket server URL
# -------------------
# 方式1: 没有域名，直接用 VPS IP（注意是 ws:// 不是 wss://）
# VITE_WS_URL=ws://your-vps-ip:1234

# 方式2: 有域名但没有 SSL
# VITE_WS_URL=ws://yourdomain.com/ws

# 方式3: 有域名且配置了 SSL（推荐）
# VITE_WS_URL=wss://yourdomain.com/ws

# 当前配置（请根据你的情况修改）:
VITE_WS_URL=wss://cocode.iziz.site/ws

# Piston API URL (代码执行服务)
# 保持默认即可，或使用你自己部署的 Piston 实例
VITE_PISTON_API_URL=https://emkc.org/api/v2/piston
